{
  "name": "oref-alerts-dashboard",
  "version": "1.0.0",
  "description": "OREF alerts dashboard with server-side polling and webhook notifications",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --mock"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
